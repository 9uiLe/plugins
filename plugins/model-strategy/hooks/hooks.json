{
  "description": "Opt-in (MODEL_STRATEGY_ROUTE_WARN=1) warning when the main session directly runs a search/read operation that references/02-decision-matrix.md routes to haiku-scout (R1). Also (conductor mode only) scope-guard warns when Edit/Write/NotebookEdit targets a file outside the frozen manifest scope.",
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Grep|Glob|Bash",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/route-warn.mjs\"",
            "timeout": 10
          }
        ]
      },
      {
        "matcher": "Edit|Write|NotebookEdit",
        "hooks": [
          {
            "type": "command",
            "command": "node \"${CLAUDE_PLUGIN_ROOT}/hooks/scope-guard.mjs\"",
            "timeout": 10
          }
        ]
      }
    ]
  }
}
