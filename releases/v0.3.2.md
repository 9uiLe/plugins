# Release Notes — v0.3.2

リリース日: 2026-07-10

> このファイルは雛形です。`## ハイライト` を手で書き加え、必要に応じて節の説明を膨らませてください。

## ハイライト

- 新プラグイン **second-opinion** (v0.1.1) を追加。Claude Code ビルトイン `advisor` 相当の第二意見レビューを、backend を **Codex / Fable** から選択（両方＝和集合も可）して得られる。現在のセッションを要約せず機械抽出し、固定 5 セクション（盲点／収束判定／ship-noship／決定的制約／最強の反論）で返す。model・effort は実行時選択。
- リリース整合性ゲート (`verify-versions.sh`) に **Claude 専用プラグインの許容機構** を追加。second-opinion のようにホスト依存で Codex に載せないプラグインを正式にサポートしつつ、登録漏れの検出能力は維持する。


### Added

- プラグイン **second-opinion** (v0.1.1) を収録 — Claude Code ビルトイン `advisor` 相当の第二意見レビュープラグイン。現在のセッションを機械抽出（要約なし。`origin.kind` で本物の人間発言のみ抽出し、全ツールエラーを verbatim、コンパクション要約は合成として明示退避）し、Codex (`codex-companion task`) と Fable (`claude -p --model claude-fable-5`) へ無編集で渡して固定 5 セクション（盲点／収束判定／ship-noship／決定的制約／最強の反論）の判定を得る。backend (codex / fable / both)・model・effort を実行時選択（無指定は AskUserQuestion）。SessionStart hook で現在の transcript を特定し、`/second-opinion:setup` で依存 (codex / claude CLI) を点検する。Claude Code 専用プラグイン。
- `scripts/verify-versions.sh` に Claude 専用プラグインの許容機構 (`CLAUDE_ONLY_PLUGINS` allowlist) を追加 — 意図的に `.codex-plugin` マニフェストと Codex マーケットプレイス登録を持たないプラグインを dual-registration 必須チェックから除外し、逆に Claude 専用なのに Codex マーケットプレイスへ登録された誤りも検出する。`write_plugin_version` とリリーススクリプトの staging も Claude 専用プラグインでは Codex マニフェストを触らないようガードした。

## Compatibility / Migration

- 破壊的変更なし。**second-opinion** は新規追加で既存プラグインに影響しない。
- **second-opinion は Claude Code 専用**。Codex ホストでは動作せず、Codex マーケットプレイスにも登録しない（Codex 側では既存 codex プラグインのレビュー機能を利用する想定）。利用には `claude` CLI（Fable backend）と、Codex backend を使う場合は openai-codex プラグインの `codex-companion.mjs` が必要。詳細は `plugins/second-opinion/README.md` を参照。
- `scripts/` と CI (`verify-versions`) の変更は内部のみで、プラグイン利用者への影響はない。

## References

- CHANGELOG: [`CHANGELOG.md`](../CHANGELOG.md)
- Compare: <https://github.com/9uiLe/plugins/compare/v0.3.1...v0.3.2>
